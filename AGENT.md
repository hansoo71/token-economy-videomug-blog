# AGENT.md

- Preserve source transcript in `docs/source/`.
- Keep claims grounded in the video transcript unless additional sources are added.
- Keep Korean executive/professional blogger tone.
- Copy generated images to `assets/images`, `src/assets/images`, and `docs/assets/images`.
- Rebuild standalone HTML in `/opt/data/out/` after changes.
